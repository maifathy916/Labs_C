#include <stdio.h>
#include <stdlib.h>
#include<string.h>
int main()
{
      /* char fname[10],lname[10],fullname[20];
       char n3;

    printf("enter first name \n");
    scanf("%s",fname);
    printf("enter last name \n");
    scanf("%s ",lname);
    strcpy(fullname,fname);
    strcat(fullname,lname);
  printf("the full name:  %s \n", fullname);
*/
/* char string[15],ch;
   int c = 0, count=0;

    printf("Enter a string\n");
    scanf("%s",string);
    printf("Enter char \n");
      ch=getch();

   while (string[c] != '\0')
    {

         if(ch==string[c])
         {
             count++;
         }

      c++;

    }
printf("freq of the string %d \n", count);

*/


  /*char ch;
   printf("enter any char : ");
  ch=getch();
if(ch==-32)
{
     ch=getch();
     printf("extended key %d",ch );
}

    printf("not extended key %d",ch);


*/

    return 0;
}
